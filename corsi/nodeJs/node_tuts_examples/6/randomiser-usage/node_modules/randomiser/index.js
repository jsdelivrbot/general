var _ = require("underscore");

var randomiser = {
  between: function(low, high) {
    return _.random(low, high);
  },
  lessThan: function(high) {
    return _.random(0, high);
  }
};

module.exports = randomiser;
